version: '2'

services:
  nginx:
    image: nginx:1.13-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./../public:/code/public

  php-fpm:
    image: php:7.1-fpm-alpine
    volumes:
      - ./../:/code
